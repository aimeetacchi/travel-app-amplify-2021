type Places @model
  @auth(rules: [{ allow: owner }]) {
    id: ID!
    city: String!
    country: String!
    description: String
    dateVisited: AWSDate
    tags: [String]
    favourite: Boolean
    visited: Boolean
    imgUrl: String!
}
